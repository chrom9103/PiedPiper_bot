apiVersion: apps/v1
kind: Deployment
metadata:
  name: piedpiper-bot
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: piedpiper-bot
  template:
    metadata:
      labels:
        app: piedpiper-bot
    spec:
      containers:
      - name: piedpiper-bot
        image: piedpiper-bot:v0.1.0
        imagePullPolicy: Never
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: database-url
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        volumeMounts:
        - name: vc-log-storage
          mountPath: /app/logs
        - name: datas-storage
          mountPath: /app/datas
      volumes:
      - name: vc-log-storage
        persistentVolumeClaim:
          claimName: vc-log-pvc
      - name: datas-storage
        persistentVolumeClaim:
          claimName: datas-pvc
